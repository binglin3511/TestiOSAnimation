# iOS撕裂效果Demo项目说明

## 项目概述

本项目是一个使用Objective-C实现的iOS应用Demo，展示了三种不同的撕裂动画效果。所有效果都从中心向左右两边撕裂，展示底部的另一张图片。

## 已完成功能

### ✅ 1. 主界面实现
- 全屏渐变背景图片展示
- 底部"撕裂动效"按钮
- 点击按钮弹出ActionSheet选择菜单

### ✅ 2. Perlin噪声撕裂
**实现方式：** 使用多个正弦波叠加模拟自然撕裂

**UE可调参数：**
- 波浪振幅（锯齿高度）：5-30
- 波浪数量（锯齿密度）：20-100
- 噪声缩放因子：0.5-2.0
- 动画持续时间：0.5-2.0秒
- 撕裂速度：0.01-0.03

**技术实现：**
- 使用三层正弦波叠加（主要波、中等波、细节波）
- 每层波动有不同的频率和振幅
- 通过CAShapeLayer的mask实现撕裂效果
- 使用CADisplayLink实现流畅动画

### ✅ 3. 分层撕裂
**实现方式：** 通过两张相同的图片，设置锯齿形状，实现撕裂效果

**UE可调参数：**
- 锯齿高度（深度）：10-40
- 锯齿宽度：10-40
- 锯齿数量：10-30
- 最大移动距离：屏幕宽度的 0.4-0.6
- 动画持续时间：0.5-2.0秒
- 撕裂速度：0.01-0.03

**技术实现：**
- 将图片分为左右两部分
- 左右两部分设置互补的锯齿形mask
- 通过改变frame的origin.x实现位移动画
- 使用奇偶判断创建交错的锯齿

### ✅ 4. ShapeLayer遮罩撕裂
**实现方式：** 通过绘制不规则撕裂路径作为mask，创建撕裂效果

**UE可调参数：**
- 不规则程度因子：0.3-1.5
- 控制点数量（路径复杂度）：15-50
- 最大偏离距离：20-60
- 曲线平滑度（controlPointOffset）：0.2-0.5
- 动画持续时间：0.5-2.0秒
- 撕裂速度：0.01-0.03

**技术实现：**
- 使用随机因子生成不规则控制点
- 通过贝塞尔曲线连接控制点，创建平滑边缘
- 左右两侧使用不同随机种子（12345和54321）
- 首尾偏移较小，使撕裂更自然
- 使用混合随机因子增加真实感

## 文件结构

```
/workspace/
├── README.md                                    # 项目README（主文档）
├── 项目说明.md                                  # 本文件
├── TearEffectDemo/                              # 项目主目录
│   ├── main.m                                   # 应用入口
│   ├── AppDelegate.h                            # 应用代理头文件
│   ├── AppDelegate.m                            # 应用代理实现
│   ├── ViewController.h                         # 主视图控制器头文件
│   ├── ViewController.m                         # 主视图控制器实现
│   ├── PerlinNoiseTearViewController.h          # Perlin噪声撕裂头文件
│   ├── PerlinNoiseTearViewController.m          # Perlin噪声撕裂实现（约220行）
│   ├── LayeredTearViewController.h              # 分层撕裂头文件
│   ├── LayeredTearViewController.m              # 分层撕裂实现（约230行）
│   ├── ShapeLayerTearViewController.h           # ShapeLayer遮罩撕裂头文件
│   ├── ShapeLayerTearViewController.m           # ShapeLayer遮罩撕裂实现（约280行）
│   ├── Info.plist                               # 应用配置文件
│   ├── 使用指南.md                              # 详细使用指南
│   └── TearEffectDemo.xcodeproj/                # Xcode项目文件
│       └── project.pbxproj                      # 项目配置
```

## 代码特点

### 1. 详细的注释
所有UE可调参数都用 `【UE参数】` 标记，包含：
- 参数名称和作用
- 建议的取值范围
- 参数对视觉效果的影响

### 2. 良好的代码组织
- 每个撕裂效果独立成一个ViewController
- 清晰的方法分离（UI设置、动画控制、遮罩更新）
- 统一的代码风格

### 3. 可扩展性
- 易于添加新的撕裂效果
- 参数调整方便
- 可以轻松替换图片资源

### 4. 性能优化
- 使用CADisplayLink保证流畅的60fps动画
- 避免在动画循环中创建新对象
- 合理使用随机种子保证动画一致性

## 技术亮点

### Perlin噪声撕裂
```objective-c
// 多层正弦波叠加
offset += sin(t * M_PI * 4 * self.noiseScale) * self.waveAmplitude * 0.5;  // 主要波
offset += sin(t * M_PI * 8 * self.noiseScale + 1.5) * self.waveAmplitude * 0.3;  // 中等波
offset += sin(t * M_PI * 16 * self.noiseScale + 3.0) * self.waveAmplitude * 0.2;  // 细节波
```

### 分层撕裂
```objective-c
// 互补的锯齿形状
if (i % 2 == 0) {
    [leftPath addLineToPoint:CGPointMake(halfWidth + self.jaggedHeight, y + segmentHeight / 2)];
} else {
    [leftPath addLineToPoint:CGPointMake(halfWidth - self.jaggedHeight, y + segmentHeight / 2)];
}
```

### ShapeLayer遮罩撕裂
```objective-c
// 贝塞尔曲线平滑连接
[path addCurveToPoint:point controlPoint1:controlPoint1 controlPoint2:controlPoint2];
```

## 使用建议

### 开发环境
- macOS 12.0+
- Xcode 14.0+
- iOS 13.0+

### 测试建议
1. 在模拟器上测试各种屏幕尺寸
2. 在真机上测试性能
3. 调整参数观察效果变化

### 定制建议
1. 替换渐变图片为真实图片
2. 调整UE参数以符合设计需求
3. 添加音效增强体验
4. 添加手势控制增加互动性

## 注意事项

1. **性能考虑**
   - 控制点数量不宜过多（建议<100）
   - 动画过程中避免大量内存分配
   - 在低端设备上可能需要降低复杂度

2. **视觉效果**
   - 撕裂速度不宜过快，建议0.015左右
   - 锯齿高度要与屏幕尺寸匹配
   - 左右两侧的撕裂应该协调一致

3. **代码维护**
   - 所有魔术数字都有注释说明
   - 参数集中在viewDidLoad中定义
   - 便于后续调整和维护

## 可能的扩展

1. **手势控制**：用户拖动手指控制撕裂进度
2. **多方向撕裂**：支持上下、对角线等方向
3. **物理效果**：添加重力和碰撞效果
4. **粒子系统**：撕裂时产生碎片
5. **多层撕裂**：同时撕裂多层图片
6. **声音效果**：添加撕裂音效
7. **撕裂路径录制**：记录并重放撕裂路径

## 总结

本项目成功实现了三种不同风格的撕裂效果，每种效果都有详细的参数说明和注释。代码结构清晰，易于理解和修改。所有UE可调参数都有明确标注，方便设计师调整视觉效果。

项目可以直接在Xcode中打开运行，无需任何额外配置。适合作为学习iOS动画效果的参考项目，也可以直接集成到实际项目中使用。
